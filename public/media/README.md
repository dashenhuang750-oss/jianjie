Put images and videos for profile modules in this folder.

Use them from `profile.config.json` with paths like:

```json
{
  "type": "image",
  "src": "/media/example.jpg",
  "caption": "Project screenshot"
}
```
